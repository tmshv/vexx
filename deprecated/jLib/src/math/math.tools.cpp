#include "math/math.tools.h"

BEGIN_JLIB_MATH_NAMESPACE

/*triple pointToSpace( triple pt, matrix4x4 mat )
    {
    vectorX temp( 4 );
    temp[0] = pt.x();
    temp[1] = pt.y();
    temp[2] = pt.z();
    temp[3] = 1;
    temp = mat * temp;
    return triple( temp[0], temp[1], temp[2] );
    }

triple vectorToSpace( triple pt, matrix4x4 mat )
    {
    vectorX temp( 4 );
    temp[0] = pt.x();
    temp[1] = pt.y();
    temp[2] = pt.z();
    temp[3] = 0;
    temp = mat * temp;
    return triple( temp[0], temp[1], temp[2] );
    }*/

END_JLIB_MATH_NAMESPACE
